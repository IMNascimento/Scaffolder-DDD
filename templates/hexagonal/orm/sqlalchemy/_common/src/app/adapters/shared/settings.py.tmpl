from pydantic_settings import BaseSettings, SettingsConfigDict
from pydantic import Field

class Settings(BaseSettings):
    # Informativo (o foundry escolhe o template certo; estes valores ainda podem ir pro .env)
    APP_ENV: str = Field(default="dev")           # dev | prod | test

    # Conexão (o significado muda levemente entre ORMs)
    DB_NAME: str = Field(default="app.db")
    DB_HOST: str | None = None
    DB_PORT: int | None = None
    DB_USER: str | None = None
    DB_PASSWORD: str | None = None

    DB_POOL_MAX: int = 10
    DB_TIMEOUT: int = 10  # segundos

    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")

settings = Settings()
